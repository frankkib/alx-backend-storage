//script that inserts a document in the collection school
db = db.getSiblingDB("my_db");  // Switch to the specified database
var result = db.school.insertOne({ name: "Holberton school" });
printjson(result);

